#include <stdio.h>

/**
 * main - main block
 * Desc: To print the English alphabets in lowercase, followed by a new line
 * Return: 0
 */
int main(x = 'A'; x <= 'Z'; x++)
{
	putchar(tolower(x));
	return (0);
}
